# Documents

**One row per document, saying what it is *for*.** The index is the one page in
`docs/` that is not itself indexed; the front page is
[`../README.md`](../README.md) and nothing here competes with it.

| document | what it is for |
| --- | --- |
| [`ai-novelty.md`](ai-novelty.md) | **Review the literature on state-of-the-art AI systems, and say how this ecosystem relates to it.** A register of one row per mechanism, the nearest outside work it would have to be measured against, and the state of that comparison — which is `unread` on every row today |
| [`science-fiction.md`](science-fiction.md) | **The upper bound: the furthest this ecosystem allows itself to plan.** Seven scenarios, ordered nearest first, each ending in something that may not be done. Marked at the top as agent-written fiction that is not to be taken seriously |
| [`maintenance.md`](maintenance.md) | **How a person maintains this repository**, and the page recording the footing this repository holds itself to. The entry point for whoever is doing the work |

**Neither of the two written documents is finished**, and neither is a result.
`ai-novelty.md` names the job that comes next: read a row of its register and
write down what the source actually says.
